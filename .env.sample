# Neon PostgreSQL — primary (production)
# Example: postgres://user:pass@ep-xxx.neon.tech/neondb?sslmode=require
DATABASE_URL=postgresql://neondb_owner:npg_Cd0qmtYg7HhZ@ep-red-cell-awxx21sz-pooler.c-12.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require

# Fallback MySQL for local dev without DATABASE_URL (optional)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=Sundari@05
DB_NAME=fuelbox_db

# JWT Secret — change this in production!
JWT_SECRET=fuelbox_jwt_secret_change_in_production_2026

# Seed endpoint secret — pass as ?secret=VALUE to protect /api/seed
SEED_SECRET=fuelbox_seed_secret_2026

# AI Advisor API Keys (server-side only — do NOT prefix with NEXT_PUBLIC_)
OPENROUTER_API_KEY=sk-or-v1-xxxxx
GROQ_API_KEY=gsk_xxxxx

# Vercel Blob — required for image uploads in production (Vercel filesystem is read-only)
# Create a Blob store at https://vercel.com/dashboard -> Storage -> Blob, then add the token to Vercel > Project > Settings > Environment Variables
# Also add to your local .env for development if testing uploads locally
BLOB_STORE_ID="store_6oMgfRvOxpRZVVlH"
BLOB_READ_WRITE_TOKEN="************"
